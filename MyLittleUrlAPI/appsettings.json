{
  "MongoConnection" : {
    "MongoClient":  "mymongodb",        // RELEASE: MongoDB & API in a container
    "MongoPort": "27017",

    // "MongoClient": "localhost",      // Debug: API in IDE (MongoDB in a container)
    // "MongoPort": "32779",

    "MongoDatabase": "MyLittleUrl",
    "MongoCollection":  "LittleUrls"
  },
  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  }
}
